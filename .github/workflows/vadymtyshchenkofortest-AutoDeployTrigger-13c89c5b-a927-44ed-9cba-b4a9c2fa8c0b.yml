name: Trigger auto deployment for vadymtyshchenkofortest

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/vadymtyshchenkofortest-AutoDeployTrigger-13c89c5b-a927-44ed-9cba-b4a9c2fa8c0b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.VADYMTYSHCHENKOFORTEST_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.VADYMTYSHCHENKOFORTEST_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.VADYMTYSHCHENKOFORTEST_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: flaskappdockerized-cxfah3evewa3hmdk.azurecr.io
          registryUsername: ${{ secrets.VADYMTYSHCHENKOFORTEST_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.VADYMTYSHCHENKOFORTEST_REGISTRY_PASSWORD }}
          containerAppName: vadymtyshchenkofortest
          resourceGroup: vadymtyshchenkofortest
          imageToBuild: flaskappdockerized-cxfah3evewa3hmdk.azurecr.io/vadymtyshchenkofortest:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


